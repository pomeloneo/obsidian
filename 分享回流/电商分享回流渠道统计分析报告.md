# 电商分享回流渠道统计分析报告

分析仓库：`/Users/bytedance/gitlab/share-it-all`  
分析目标：只从「回流回来」角度统计分享带来的回流 UV/PV，覆盖 PDP 商品详情页、店铺详情页、电商直播间、电商短视频。  
口径原则：回流看板优先使用接收端/落地端事件。发送侧事件只用于补充具体渠道归因，不把“点击分享渠道/分享成功”直接当成“回流回来”。

## 总结

回流统计要先分清两个维度：

| 维度 | 含义 | 是否可以直接从落地事件拿到 |
| --- | --- | --- |
| `reflow_channel_group` | 回流大类：站内 IM、站外分享链接、其他/未知 | PDP、店铺、短视频 IM/站外基本可以；直播站外需用样本确认 `livesdk_live_play` 是否落表透传字段 |
| `source_platform` | 具体发送渠道：WhatsApp、Facebook、Messenger、SMS、TikTok IM 等 | 大多数场景不能只靠落地事件。要和发送侧分享事件按 `chain_key`、`share_link_id`、`shareLiveTraceId` 或时间窗 join |

推荐看板主表按下面字段建：

| 字段 | 说明 |
| --- | --- |
| `scene` | `pdp` / `shop` / `live` / `short_video` |
| `landing_event` | 回流落地事件名 |
| `reflow_channel_group` | `im` / `outside` / `other` / `unknown` |
| `source_platform` | 有可靠 join 或落地透传时再填；否则填 `unknown`，不要硬拆 |
| `object_id` | PDP 用 `product_id`，店铺用 `shop_id`，直播用 `room_id`，短视频用 `group_id/item_id` |
| `receiver_user_id` | 回流用户，不是分享发起用户 |
| `share_trace_id` | 可用时填 `chain_key`、`share_link_id`、`from_unique_share_id/shareLiveTraceId` |
| `landing_pv` / `landing_uv` | 回流 PV / UV |

## 场景总表

| 场景        | 回流主事件                                                      | 站内 IM 回流过滤                                                                      | 站外回流过滤                                                                                                             | 能否直接拆具体站外 App                                                                |
| --------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| PDP 商品详情页 | `tiktokec_enter_product_detail`（代码确认，抓包/数仓需先校验；未见时看下方兜底口径） | `enter_from_info = 'product_share_im'` 或 `entrance_form = 'product_share_card'` | `enter_from_info = 'product_share_outside'`                                                                        | 不能。只能直接拆 IM vs 站外；具体 App 需 join `tiktokec_share_product.platform`            |
| 店铺详情页     | `tiktokec_enter_shop`                                      | `enter_from = 'chat_im'`                                                        | `enter_from = 'shop_share_outside'`                                                                                | 不能。具体 App 需 join `tiktokec_shop_share_success.channel_id`                    |
| 电商直播间     | `livesdk_live_play`                                        | `is_ecom > 0` 且 `enter_from_merge = 'chat'`；前置可看 `livesdk_share_chat_click`     | 外部链接进房代码透传 `from_share_platform`、`share_from_user_id`，建议先在 `livesdk_live_play` 样本确认字段；确认后按 `from_share_platform` 拆 | 部分可行。`from_share_platform` 落表时可直接拆；否则 join `livesdk_share_v2.share_platform` |
| 电商短视频     | `video_play`                                               | `enter_from = 'chat'` 且 `is_from_dm = 1`                                        | `share_link_id` 非空，或 `ugdp_type` 落表；`ugdp_id` 只作链路 ID，不单独作为分享过滤                                                    | 不能。`ugdp_type` 是打开方式，不是 App；具体 App 需 join `share_video.platform`             |

## PDP 商品详情页

PDP 回流候选主事件：

```sql
event = 'tiktokec_enter_product_detail'
```

重要修订：代码里 SEA/Global PDP 的 `trackPageShow` 都会通过 `TTEC_TRACK_REPORT_WITH_PARAMS(@"tiktokec_enter_product_detail", trackInfo)` 上报这个事件；但如果实际抓包没有看到，就不能把它直接作为已落表主口径上线。PDP 回流看板需要先在真实日志/数仓样本里确认该事件是否可查到、是否带 `enter_from_info` / `entrance_form` / `product_id` / `chain_key` 这些字段。

如果抓包或数仓样本确认看不到 `tiktokec_enter_product_detail`，PDP 回流口径按下面顺序降级：

| 优先级 | 事件 | 用途 | 能否直接当 PDP 回流 UV |
| --- | --- | --- | --- |
| P0 | `tiktokec_enter_product_detail` | PDP 页面 show，代码确认的标准落地事件 | 可以，但前提是数仓/抓包确认存在并带分享来源字段 |
| P1 | `tiktokec_internal_enter_page_attribution_event` | PDP 内部归因事件，紧跟页面进入链路上报，参数来自 schema/UG trace | 可以作为候选替代，但必须先确认带 `enter_from_info`、`entrance_form` 或分享 trace |
| P2 | `tiktokec_stay_product_detail` | PDP 停留/消费事件，说明用户实际进入并停留过商品详情 | 只能作为“有效回流/停留回流”兜底，不等价于首次落地 PV |
| 诊断 | `rd_tiktokec_product_detail_request_send` / `rd_tiktokec_product_detail_request_result` | PDP 请求发起/返回，适合定位链路和请求成功率 | 不建议当业务回流 UV 主口径 |

因此实际建看板时先做一张 PDP 回流事件校验表：

```sql
event in (
  'tiktokec_enter_product_detail',
  'tiktokec_internal_enter_page_attribution_event',
  'tiktokec_stay_product_detail',
  'rd_tiktokec_product_detail_request_send',
  'rd_tiktokec_product_detail_request_result'
)
and (
  enter_from_info in ('product_share_outside', 'product_share_im')
  or entrance_form = 'product_share_card'
  or json_extract_scalar(trackParams, '$.enter_from_info') in ('product_share_outside', 'product_share_im')
  or json_extract_scalar(trackParams, '$.entrance_form') = 'product_share_card'
)
```

确认哪一个事件在真实日志里稳定出现后，再把它写入最终看板口径。不要只因为代码里存在 `tiktokec_enter_product_detail` 就直接上线。

### PDP 无页面事件时的通用唤端兜底

如果上面 PDP 页面层事件都没有，还可以降到通用唤端/DeepLink 层统计“站外分享链接把 App 拉起并完成路由”的回流。这个口径只能覆盖站外链接回流，不能覆盖站内 IM 卡片在 App 内点击的回流。

可用事件按可信度排序：

| 优先级 | 事件 | 能证明什么 | 关键字段/过滤 |
| --- | --- | --- | --- |
| L0 | `deep_link_end` | DeepLink 路由结束；`success = 1` 时说明路由链路成功完成 | `success = 1`；`ug_source like 'share.%'` 或 URL 中有 `utm_campaign=client_share`；用 `jump_url` / `start_url` / `url` / `enter_to` 识别 PDP |
| L1 | `launch_log` | App 被外部链接/Universal Link/Scheme 拉起并进入 active | `is_share_link_launch = '1'` 或 `utm_campaign = 'client_share'`；`link_id` 是 `share_link_id`；`platform` / `utm_source` 可作渠道 |
| L2 | `deep_link_start` | App 开始处理 DeepLink | `url` 带分享参数；`ugdp_id` 可和 `deep_link_end` 串联 |
| L3 | `deeplink_system_cb` | iOS 系统回调收到了 openURL / Universal Link | `open_url`、`deep_link_source_type`；只作诊断，不建议当回流 UV |

代码依据：

```text
launch_log:
TikTokUITracker/Classes/Core/AppDelegateTracker/AWEAppDelegateTrackManager.m
trackEvent:@"launch_log"

deep_link_start / deep_link_end / deeplink_system_cb:
TikTokExternalRouter/AWEExternalRouter/Classes/Core/TTKDeeplinkTracker.m
```

`launch_log` 会把原始 URL query、`params_url` 解析出的 query、以及启动公共字段合并上报。重点字段：

| 字段 | 说明 |
| --- | --- |
| `launch_method` / `ug_launch_method` | 外链拉起方式，常见 `link_direct`、`link_share`、`retarget`、`appsflyer` |
| `is_share_link_launch` | 代码通过 `utm_campaign = client_share` 判断是否分享链接拉起 |
| `link_id` | 代码从 `share_link_id` 写入，可作为分享链路 ID |
| `ugdp_id` | Deeplink 唯一 ID，可和 `deep_link_start/end` 串联 |
| `platform` / `utm_source` | 外部平台来源，能否等同 WhatsApp/Facebook 需用样本与发送侧 `platform` 对齐 |
| `params_url` / `ug_launch_url` / `convert_url` / `referrer_url` | 原始/承接 URL，用于识别 PDP、店铺、直播、短视频目标页 |
| `from_user_id` | URL 透传用户 ID，可能是分享发起人，但必须用样本确认语义 |
| `enter_to` | 通用入口目标；电商 PDP 可能不会被规范化成 `product_detail`，需要直接解析 URL |

通用唤端兜底 SQL 示例：

```sql
-- 站外分享链接唤端成功，偏“App 拉起”口径
event = 'launch_log'
and (
  is_share_link_launch = '1'
  or utm_campaign = 'client_share'
  or lower(coalesce(params_url, ug_launch_url, convert_url, o_url, '')) like '%utm_campaign=client_share%'
)
and (
  lower(coalesce(params_url, ug_launch_url, convert_url, o_url, '')) like '%ec/pdp%'
  or lower(coalesce(params_url, ug_launch_url, convert_url, o_url, '')) like '%product%'
)

-- 站外分享链接 DeepLink 路由成功，偏“路由成功”口径
event = 'deep_link_end'
and success = 1
and (
  ug_source like 'share.%'
  or lower(coalesce(url, start_url, jump_url, '')) like '%utm_campaign=client_share%'
)
and (
  lower(coalesce(jump_url, start_url, url, '')) like '%ec/pdp%'
  or lower(coalesce(jump_url, start_url, url, '')) like '%product%'
)
```

这个兜底口径的看板命名必须写清楚：

```text
PDP 站外分享唤端 UV
PDP 站外分享 DeepLink 路由成功 UV
```

不要命名成“PDP 落地 UV”或“PDP 页面回流 UV”。如果没有 PDP 页面层事件，只能证明外链进入 App 或路由成功，不能证明 PDP 页面完成渲染。

### 回流大类

| 回流大类 | 过滤条件 | 说明 |
| --- | --- | --- |
| 站外回流 | `enter_from_info = 'product_share_outside'`，或 `json_extract_scalar(trackParams, '$.enter_from_info') = 'product_share_outside'` | 普通用户从站外分享链接回到 PDP。 |
| 站内 IM 回流 | `enter_from_info = 'product_share_im'`，或 `entrance_form = 'product_share_card'`，或对应 `trackParams` JSON 字段 | IM 商品卡点击回到 PDP。 |

推荐 SQL 过滤。下面写法只在确认 `tiktokec_enter_product_detail` 真实落表后使用：

```sql
-- PDP 站外回流
event = 'tiktokec_enter_product_detail'
and (
  enter_from_info = 'product_share_outside'
  or json_extract_scalar(trackParams, '$.enter_from_info') = 'product_share_outside'
)

-- PDP IM 回流
event = 'tiktokec_enter_product_detail'
and (
  enter_from_info = 'product_share_im'
  or entrance_form = 'product_share_card'
  or json_extract_scalar(trackParams, '$.enter_from_info') = 'product_share_im'
  or json_extract_scalar(trackParams, '$.entrance_form') = 'product_share_card'
)
```

去重建议：

```text
UV: distinct receiver_user_id
对象回流 UV: distinct receiver_user_id, product_id
链路级回流 UV: distinct receiver_user_id, product_id, chain_key
```

### 渠道拆分边界

PDP 落地侧只能稳定拆出：

```text
product_share_outside
product_share_im
```

不要直接把 `product_share_outside` 拆成 WhatsApp/Facebook/Messenger 等具体 App。具体 App 是发送侧的 `tiktokec_share_product.platform`，要做“各站外 App 回流”时必须 join：

```text
发送侧：tiktokec_share_product(chat_type = 'outside', platform)
落地侧：tiktokec_enter_product_detail(enter_from_info = 'product_share_outside')，前提是该事件在真实日志里已确认落表；否则改用 PDP 章节校验后的最终落地事件
关联键优先：chain_key；没有 chain_key 再退化 product_id + sender/receiver/time window
```

注意：`shop_share_reflow` 不是 PDP 回流指标，不要用于 PDP 看板。

### PDP 回流承载方式

PDP 回流不只有“全屏 PDP”一种展示形态。PDP 自身埋点里会带 `page_show_type`，常见取值是：

```text
full_screen
half_screen
```

这里的 `half_screen` 就是业务上常说的 PDP 半屏/七分屏承载。PDP 分享回流 P0 候选口径仍然先看：

```text
tiktokec_enter_product_detail
enter_from_info = product_share_outside / product_share_im
```

代码里还存在“先进入商城，再由商城承接落地页打开 PDP 半屏/七分屏”的路径。`TTKECMallEntryDataUpdateActions` 会处理 `landingPageSchema` 为 `ec/pdp` 的情况：

```text
landingPageSchema endsWith ec/pdp
fullScreen = true  -> full screen PDP
fullScreen = false -> half screen PDP
```

如果 `tiktokec_enter_product_detail` 在抓包/数仓里未出现，则同样先按上文的 P1/P2 事件校验，不要只按代码事件名写看板。

同时这条商城承接路径会按当前商城入口把 PDP 的 `trackParams.enter_from` / `trackParams.enter_from_info` 写成动态来源：

```text
mall
homepage_mall
```

因此取数时要分两层：

| 口径           | 推荐过滤                                                                                                        | 说明                                                                                                                                      |
| ------------ | ----------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| PDP 分享直达回流   | 最终选定的 PDP 回流事件 + `enter_from_info in ('product_share_outside', 'product_share_im')` 或 `entrance_form = 'product_share_card'` | PDP 分享回流口径。优先用 P0；P0 未落表时用已校验 P1/P2。                                                                                                                            |
| PDP 分享回流承载形态 | 在最终选定的 PDP 回流事件上再拆 `page_show_type = 'full_screen' / 'half_screen'`                                                    | 用来区分全屏 PDP 和半屏/七分屏 PDP；如果兜底事件没有 `page_show_type`，该拆分不能做。                                                                                                                 |
| 商城承接 PDP 半屏  | `enter_from_info in ('mall', 'homepage_mall')` 且 `page_show_type = 'half_screen'`                           | 这是商城流量承接 PDP，不要直接并入 PDP 分享回流。只有样本里还能通过 `chain_key`、`source_page_type='product_share'` 或其他分享 trace 证明来自 PDP 分享时，才可单独作为“PDP 分享经商城承接回流”分支。 |

也就是说：可以有“商城首页 + PDP 七分屏”的承载方式，但不要把所有 `homepage_mall/mall + half_screen PDP` 都算作 PDP 分享回流。先在最终选定的 PDP 回流事件上用 `product_share_outside/product_share_im` 做直达回流，再用 `page_show_type` 拆承载；如果要覆盖商城承接的分享回流，需要先用真实日志确认分享 trace 是否保留下来。

## 店铺详情页

店铺详情页回流主事件：

```sql
event = 'tiktokec_enter_shop'
```

### 回流大类

| 回流大类     | 用户级主口径                                                  | 服务端对账口径                                          | 说明                |
| -------- | ------------------------------------------------------- | ------------------------------------------------ | ----------------- |
| 站外回流     | `tiktokec_enter_shop.enter_from = 'shop_share_outside'` | `shop_share_reflow(type = 'shop_share_outside')` | C 端店铺详情页站外分享链接回流。 |
| 站内 IM 回流 | `tiktokec_enter_shop.enter_from = 'chat_im'`            | `shop_share_reflow(type = 'chat_im')`            | 店铺 IM 卡/消息回流。     |

推荐 SQL 过滤：

```sql
-- 店铺站外回流
event = 'tiktokec_enter_shop'
and enter_from = 'shop_share_outside'

-- 店铺 IM 回流
event = 'tiktokec_enter_shop'
and enter_from = 'chat_im'
```

服务端对账：

```text
metric = shop_share_reflow
type in ('shop_share_outside', 'chat_im')
```

`shop_share_reflow(type='seller_qrscan')` 是 B 端/Seller QR 回流，不属于 C 端店铺详情页右上角 Share 主链路；做 C 端分享回流看板时要排除。

### 渠道拆分边界

店铺落地侧能直接拆：

```text
shop_share_outside
chat_im
```

不能只靠 `tiktokec_enter_shop` 拆 WhatsApp/Facebook/Messenger/SMS。具体发送渠道来自发送侧：

```text
tiktokec_shop_share_success.channel_id
```

已确认站内 IM 发送侧枚举：

```text
chat_merge
chat_more
```

其他如 `messenger`、`sms`、`email`、`instagram_dm`、`snapchat_chats` 虽然有“消息/私信”语义，但它们是站外 App/系统渠道，不是 TikTok 站内 IM。

## 电商直播间

电商直播回流进入主事件：

```sql
event = 'livesdk_live_play'
and cast(is_ecom as int) > 0
```

如果只看明确有商品/挂车直播间，可以收窄为：

```sql
event = 'livesdk_live_play'
and cast(is_ecom as int) = 1
```

### 站内 IM 回流

直播 IM 回流链路中有三层可看：

| 层级 | 事件 | 过滤 | 用途 |
| --- | --- | --- | --- |
| IM 卡曝光 | `livesdk_share_chat_show` | `room_id`、`from_user_id`、`chat_type` | 接收端看到直播分享卡。 |
| IM 卡点击 | `livesdk_share_chat_click` | `room_id`、`from_user_id`、`chat_type` | 接收端点击直播分享卡。 |
| 实际进房 | `livesdk_live_play` | `is_ecom > 0` 且 `enter_from_merge = 'chat'` | 回流进入直播间主口径。 |

辅助曝光事件：

```sql
event = 'livesdk_live_show'
and enter_from_merge = 'chat'
and enter_method = 'live_cover'
```

`livesdk_live_show` 是 IM 直播卡/入口曝光，不是最终进房；最终回流 UV/PV 用 `livesdk_live_play`。

如果 IM 分享消息里带 `shareLiveTraceId`，接收端 `livesdk_live_show` 会带：

```text
from_unique_share_id = shareLiveTraceId
```

这个字段适合把 IM 卡曝光/点击与发送侧分享链路串起来。

### 站外回流

站外直播分享链接进入房间的代码侧特征：

```text
URL/params 同时带 share_app_id 和 share_link_id -> isFromShare = true
contextParams 透传 from_share_platform
contextParams 透传 share_from_user_id
utm_campaign = client_share 时 roomLog.shareSourceType = external_link
```

推荐先在日志样本里确认 `livesdk_live_play` 是否落表以下字段：

```text
from_share_platform
share_from_user_id
from_user_id
enter_from
enter_from_merge
enter_method
```

确认字段落表后，站外回流可按下面口径建：

```sql
event = 'livesdk_live_play'
and cast(is_ecom as int) > 0
and (
  from_share_platform is not null and from_share_platform <> ''
  or share_from_user_id is not null and share_from_user_id <> ''
  or from_user_id is not null and from_user_id <> ''
)
```

如果 `from_share_platform` 有值，可以作为直播站外具体渠道；如果没有值，只能统计“站外分享链接回流”，不能拆到 App。具体渠道要 join 发送侧：

```text
发送侧：livesdk_share_v2.share_platform
落地侧：livesdk_live_play 外部链接回流字段
关联键优先：share_link_id/share_app_id 或 shareLiveTraceId；没有稳定 ID 时只能做时间窗归因，不能作为精确渠道回流
```

## 电商短视频

短视频回流落地第一跳主看播放事件：

```sql
event = 'video_play'
```

电商短视频过滤：

```sql
-- 严格商品视频
is_ecom = 1

-- 更宽的电商视频/电商意图视频，可作为扩展口径
is_ec_video = 1
or cast(is_ecom_intent as int) > 0
or ec_product_cnt > 0
```

### 站内 IM 回流

IM 中打开短视频的落地参数来自聊天内打开 feed 的链路，已确认会写入：

```text
refer = 'chat'
enter_from = 'chat'
is_from_dm = 1
enter_position = 'video_cover'
chat_type
conversation_id
```

推荐 IM 回流过滤：

```sql
event = 'video_play'
and is_ecom = 1
and enter_from = 'chat'
and is_from_dm = 1
```

如果要拆私聊/群聊：

```text
chat_type = 'private' / 'group'
conversation_id
```

### 站外回流

站外短视频分享链接回流会走 deeplink 插入视频逻辑，`video_play` 上会带：

```text
ugdp_id
share_link_id
link_sharer_uid
sharer_info_req_id
ugdp_type
insert_aweme_duration
```

推荐站外回流过滤：

```sql
event = 'video_play'
and is_ecom = 1
and (
  share_link_id is not null and share_link_id <> ''
  or ugdp_type is not null
)
```

注意 `ugdp_type` 不是 WhatsApp/Facebook 这类分享 App，它是打开方式枚举：

```text
1 = AppsFlyer conversion
2 = Universal Link
3 = Scheme
-1 = other
```

因此短视频“站外各 App 回流”不能只用 `video_play.ugdp_type`。要拆具体 App，需要 join 发送侧：

```text
发送侧：share_video(platform, share_link_id)
落地侧：video_play(share_link_id, ugdp_id, ugdp_type)
关联键优先：share_link_id；没有 share_link_id 时不要做精确 App 回流
```

`play_time` 和 `video_play_finish` 也会补充消费侧回流字段：

```text
external_sharer_uid
_oid
```

它们适合看回流后的观看质量，不建议替代 `video_play` 作为“回流进入”的第一跳主口径。

## 推荐看板结构

### 看板 1：跨场景回流总览

| 指标 | 口径 |
| --- | --- |
| 回流 PV | 各场景落地事件 PV |
| 回流 UV | 各场景落地事件 receiver `user_id` 去重 |
| 回流对象数 | `product_id` / `shop_id` / `room_id` / `group_id` 去重 |
| IM 回流 UV | `reflow_channel_group = 'im'` |
| 站外回流 UV | `reflow_channel_group = 'outside'` |
| 未知回流 UV | 符合分享回流迹象但无法稳定归类的样本，单独观察 |

### 看板 2：分场景渠道回流

| scene | im 口径 | outside 口径 | detail channel 口径 |
| --- | --- | --- | --- |
| PDP | `enter_from_info='product_share_im'` 或 `entrance_form='product_share_card'` | `enter_from_info='product_share_outside'` | join `tiktokec_share_product.platform` |
| Shop | `enter_from='chat_im'` | `enter_from='shop_share_outside'` | join `tiktokec_shop_share_success.channel_id` |
| Live | `livesdk_live_play.enter_from_merge='chat'` | `from_share_platform/share_from_user_id/from_user_id` 透传字段命中 | `from_share_platform` 或 join `livesdk_share_v2.share_platform` |
| Short video | `video_play.enter_from='chat' and is_from_dm=1` | `video_play.share_link_id` 非空或 `ugdp_type` 落表 | join `share_video.platform` |

### 看板 3：回流质量

| 场景 | 回流后质量指标 |
| --- | --- |
| PDP | 回流 PDP 后商品点击、加购、下单；按 `product_id + chain_key` 串联 |
| Shop | 回流店铺后停留、商品点击、关注/进店转化；按 `shop_id + enter_from` 串联 |
| Live | 回流进房后停留、商品曝光/点击、下单；用 `room_id + live_play_session_id` 串联 |
| Short video | `play_time`、`video_play_finish`、商品锚点曝光/点击；用 `group_id + share_link_id/ugdp_id` 串联 |

## 从分享到回流的漏斗看板搭建

这个看板不要做成传统的“同一个 user_id 从点击分享到回流”的漏斗。分享侧是发送者，回流侧是接收者，两个 `user_id` 通常不同。建议搭成两张明细事实表，再按 trace 做归因：

```text
share_source_base  -- 发送侧：谁分享了什么、分享到哪个渠道
reflow_landing_base -- 回流侧：谁从分享链路回来了、落到哪个页面
attribution_base   -- 用 chain_key/share_link_id/shareLiveTraceId 等把两侧串起来
dashboard_agg      -- 按 scene/channel/platform/object/date 聚合
```

### 统一字段

发送侧表建议字段：

| 字段 | 说明 |
| --- | --- |
| `scene` | `pdp` / `shop` / `live` / `short_video` |
| `source_event` | 发送侧事件名 |
| `sender_user_id` | 分享发起用户 |
| `object_id` | `product_id` / `shop_id` / `room_id` / `group_id` |
| `channel_group` | `im` / `outside` / `toolbar` |
| `source_platform` | 原始渠道，如 `chat`、`chat_merge`、`whatsapp`、`facebook`、`copy_link` |
| `share_trace_id` | `chain_key` / `share_link_id` / `shareLiveTraceId`，没有就置空 |
| `share_target_cnt` | IM 可用 `chat_cnt` / recipient count；站外通常按 share action PV |
| `source_ts` | 分享时间 |

回流侧表建议字段：

| 字段 | 说明 |
| --- | --- |
| `scene` | 回流落地场景 |
| `landing_event` | 回流落地事件名 |
| `receiver_user_id` | 回流用户 |
| `object_id` | 回流对象 |
| `reflow_channel_group` | `im` / `outside` / `mall_carried` / `unknown` |
| `landing_platform` | 落地侧能直接拿到具体渠道时再填；否则 `unknown` |
| `share_trace_id` | 落地侧保留下来的 trace |
| `page_show_type` | PDP 可拆 `full_screen` / `half_screen` |
| `landing_ts` | 回流时间 |

归因分三档：

| 归因等级 | 规则 | 能否做具体渠道回流 |
| --- | --- | --- |
| P0 精确归因 | `share_trace_id` 能一一 join，例如 `chain_key`、`share_link_id`、`from_unique_share_id/shareLiveTraceId` | 可以 |
| P1 半精确归因 | 落地事件直接带渠道字段，例如直播站外 `from_share_platform` | 可以，但要先确认字段落表 |
| P2 聚合归因 | 没有稳定 trace，只能按 `scene + object_id + channel_group + 时间窗` 聚合比较 | 只能做站内/站外总量，不做具体 App 回流率 |

时间窗建议至少做三档：

```text
D0: landing_ts between source_ts and source_ts + 24h
D1: landing_ts between source_ts and source_ts + 48h
D7: landing_ts between source_ts and source_ts + 7d
```

跨时区看板统一用业务数仓日期，不要把发送侧和回流侧分别按本地日期切开后再相除。

### 看板 A：各渠道分享 -> 回流总览

维度：

```text
date
scene
channel_group
source_platform
attribution_level
```

指标：

| 指标 | 口径 |
| --- | --- |
| 分享 PV | `share_source_base` 行数 |
| 分享 UV | `sender_user_id` 去重 |
| 分享对象数 | `object_id` 去重 |
| 可归因分享 PV | `share_trace_id` 非空，或落地侧有可用渠道字段 |
| 回流 PV | `reflow_landing_base` 行数 |
| 回流 UV | `receiver_user_id` 去重 |
| 回流对象数 | `object_id` 去重 |
| D0/D1/D7 回流率 | `回流 UV / 分享 PV` 或 `回流 UV / share_target_cnt`；IM 优先用 target cnt，站外通常用分享 PV |

注意：站外分享 PV 只是“用户选择/执行站外渠道”的客户端口径，不等于第三方 App 内真实投递或真实曝光。因此站外回流率更准确地叫：

```text
站外分享动作到回流率
```

### 看板 B：PDP 商品详情页分享 -> PDP 回流

推荐分三条分支：

| 分支                    | 发送侧漏斗                                                                                                                                                               | 回流侧漏斗                                                                                                      | 归因方式                                                                            |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| PDP -> 站内 IM -> PDP   | `tiktokec_button_click(button_name='product_share')` -> `tiktokec_share_product(platform='chat')` -> `tiktokec_share_product_to_chat(message_type='product_share')` | P0: `tiktokec_enter_product_detail(enter_from_info='product_share_im' or entrance_form='product_share_card')`；若抓包/数仓未见，降级看 `tiktokec_internal_enter_page_attribution_event`，再看 `tiktokec_stay_product_detail` | 优先 `chain_key`；没有则只做 IM 总量 |
| PDP -> 站外 App -> PDP  | `tiktokec_button_click(button_name='product_share')` -> `tiktokec_share_product(chat_type='outside', platform)`                                                     | P0: `tiktokec_enter_product_detail(enter_from_info='product_share_outside')`；若 PDP 页面层事件都没有，降到 `deep_link_end(success=1)` / `launch_log(is_share_link_launch='1')` 做“唤端/路由成功”口径 | 优先 `chain_key/share_link_id/link_id/ugdp_id`；否则具体 App 不做精确回流 |
| PDP -> 商城承接 -> PDP 半屏 | 发送侧同 PDP 分享                                                                                                                                                         | P0: `tiktokec_enter_product_detail(page_show_type='half_screen', enter_from_info in ('mall','homepage_mall'))`；若未见同样按 PDP 兜底事件校验 | 只有保留 `chain_key/source_page_type='product_share'` 时才归入 PDP 分享；否则作为商城承接 PDP 流量单列 |

PDP 看板建议指标：

| 指标 | 口径 |
| --- | --- |
| PDP 分享点击 UV | `tiktokec_button_click(button_name='product_share')` |
| IM 发送 UV | `tiktokec_share_product_to_chat(message_type='product_share')` |
| 站外分享 PV/UV | `tiktokec_share_product(chat_type='outside')`，按 `platform` 拆 |
| IM 回流 PDP UV | 优先 `tiktokec_enter_product_detail` + IM 回流过滤；未落表时用已校验的 P1/P2 兜底事件 |
| 站外回流 PDP UV | 优先 `tiktokec_enter_product_detail` + outside 回流过滤；未落表时用已校验的 P1/P2 兜底事件 |
| 站外分享唤端 UV | PDP 页面事件都没有时，使用 `launch_log(is_share_link_launch='1')` 或 `deep_link_end(success=1, ug_source like 'share.%')`，并解析 URL 指向 PDP |
| PDP 半屏/七分屏回流占比 | 在最终选定的 PDP 回流事件上按 `page_show_type` 拆；如果兜底事件不带该字段，就不能拆承载形态 |

PDP 关键限制：如果落地侧只有 `product_share_outside`，没有 `chain_key` 或可 join trace，就只能知道“站外回流”，不能知道是 WhatsApp 还是 Facebook 回来的。另一个限制是：如果真实日志里没有 `tiktokec_enter_product_detail`，最终看板必须明确标注所用的是 `tiktokec_internal_enter_page_attribution_event`、`tiktokec_stay_product_detail` 或通用 `launch_log/deep_link_end` 兜底口径，不能混写成 PDP enter 事件。

### PDP PV 漏斗低于 UV 漏斗的排查

如果看板上出现“PV 漏斗比 UV 漏斗更低”，先确认比较的是百分比还是绝对数。

如果比较的是截图里的漏斗转化率，这是正常现象。PV 漏斗分母是 PDP 进入/曝光次数，UV 漏斗分母是去重用户数。PDP 是高频浏览页，同一个用户可能一天进入很多个 PDP、同一个 PDP 也可能反复打开，但分享动作通常很少发生，所以：

```text
UV 转化率 = 分享用户数 / 进入 PDP 用户数
PV 转化率 = 分享动作次数 / 进入 PDP 次数
```

只要人均 PDP 进入次数明显大于人均分享次数，PV 转化率就会显著低于 UV 转化率。例如 100 个用户共进入 PDP 1000 次，只有 10 个用户各分享 1 次：

```text
UV 分享率 = 10 / 100 = 10%
PV 分享率 = 10 / 1000 = 1%
```

如果比较的是绝对数，理论上同一个事件、同一过滤、同一时间窗下，原始 PV 不应该小于 UV。出现绝对 PV < UV 时，优先查这些问题：

| 排查点 | 可能原因 |
| --- | --- |
| 看板“PV”不是原始事件 PV | 一些漏斗工具里的 PV 是“完成路径次数”或“转化次数”，不是单步事件行数。严格按顺序、时间窗、session、chain 组装后，路径次数可能小于 UV。 |
| PV/UV 两张看板步骤不完全一致 | 例如 PV 漏斗多了“直接回流到 PDP 详情页”终点，UV 漏斗只到“分支步骤”；路径越长，转化率越低。 |
| 分母事件不一样 | `进入电商详情页上报`、`PDP 顶部分享按钮展示`、`分享按钮点击` 如果分别来自不同事件或不同组件，PV/UV 不能直接横比。 |
| 过滤条件不一致 | 地区、端、版本、页面形态、`page_show_type`、`enter_from_info`、商品可分享状态、抽样方式不同，都会让 PV/UV 看起来反常。 |
| 路径归因过严 | PV 漏斗如果要求同一个 `page_view_id`、`chain_key`、`session_id` 或 10 分钟内严格顺序，很多跨页面/跨会话分享会被丢掉。 |
| 回流终点事件缺失 | PDP 页面层事件抓不到时，“直接回流到 PDP 详情页”这一步会偏低甚至为 0；这时只能单列 `launch_log/deep_link_end` 的唤端/路由成功口径。 |
| 分支去重字段影响 | 站内/站外分支如果用 `unique_id`、`share_link_id`、`chain_key` 去重，而前面步骤按事件 PV 计数，分支 PV 会被压低。 |

建议排查顺序：

```text
1. 先导出每一步的绝对 event_count 和 user_count，不只看百分比。
2. 确认 PV 漏斗的 PV 是 raw event PV，还是 funnel path count。
3. 确认 UV/PV 两张看板的事件、过滤、时间窗、路径顺序完全一致。
4. 单独跑每个 step 的原始 SQL，看单步 PV 是否都 >= UV。
5. 如果只有回流终点异常低，优先怀疑 PDP 落地事件缺失，改用通用唤端兜底口径单列。
```

### 看板 C：店铺详情页分享 -> 店铺回流

推荐分两条主分支：

| 分支 | 发送侧漏斗 | 回流侧漏斗 | 归因方式 |
| --- | --- | --- | --- |
| 店铺 -> 站内 IM -> 店铺 | `tiktokec_module_click(module_name='share')` -> `tiktokec_shop_share_success(channel_id in ('chat_merge','chat_more'))` | `tiktokec_enter_shop(enter_from='chat_im')`，辅看 `shop_share_reflow(type='chat_im')` | 本地代码未确认稳定 share trace，默认做 IM 总量 |
| 店铺 -> 站外 App -> 店铺 | `tiktokec_module_click(module_name='share')` -> `tiktokec_shop_share_success(channel_id not in ('chat_merge','chat_more'))` | `tiktokec_enter_shop(enter_from='shop_share_outside')`，辅看 `shop_share_reflow(type='shop_share_outside')` | 具体 App 需要发送侧 `channel_id` 与落地 trace/join；没有 trace 时只做站外总量 |

店铺看板建议指标：

| 指标 | 口径 |
| --- | --- |
| 店铺分享点击 UV | `tiktokec_module_click(module_name='share')` |
| 店铺分享成功 UV | `tiktokec_shop_share_success` |
| IM 分享成功 UV | `channel_id in ('chat_merge','chat_more')` |
| 站外分享成功 UV | `channel_id not in ('chat_merge','chat_more')` |
| IM 回流店铺 UV | `tiktokec_enter_shop(enter_from='chat_im')` |
| 站外回流店铺 UV | `tiktokec_enter_shop(enter_from='shop_share_outside')` |
| 服务端回流 PV 对账 | `shop_share_reflow(type in ('chat_im','shop_share_outside'))` |

店铺不要把 `seller_qrscan` 并入 C 端分享回流。它是 Seller/QR 口径。

### 看板 D：电商直播分享 -> 直播回流

推荐分三条分支：

| 分支 | 发送侧漏斗 | 回流侧漏斗 | 归因方式 |
| --- | --- | --- | --- |
| 直播 -> 站内 IM 卡曝光/点击 -> 进房 | `livesdk_user_share_click` / `livesdk_anchor_share_click` -> `livesdk_share_v2(share_platform in ('chat','chat_merge'))`，辅看 `share_video_to_chat` | `livesdk_share_chat_show` -> `livesdk_share_chat_click` -> `livesdk_live_play(is_ecom>0, enter_from_merge='chat')` | `from_unique_share_id/shareLiveTraceId` 可用时精确，否则按 IM 总量 |
| 直播 -> 站外 App -> 进房 | `livesdk_share_v2(to_user_id='out_app_share' or share_platform not in ('chat','chat_merge','bulletin_board'))` | `livesdk_live_play(is_ecom>0)` 且命中 `from_share_platform/share_from_user_id/from_user_id` | `from_share_platform` 落表时可直接拆；否则需要 join |
| 直播 -> 分享卡曝光未进房 | 同 IM 发送侧 | `livesdk_share_chat_show` / `livesdk_share_chat_click`，未命中后续 `livesdk_live_play` | 用于诊断 IM 卡点击到进房损耗 |

直播看板建议指标：

| 指标 | 口径 |
| --- | --- |
| 电商直播间分享点击 UV | 点击分享入口且 `room_id` 在 `livesdk_live_play(is_ecom>0)` 房间集合内 |
| IM 分享 PV/UV | `livesdk_share_v2.share_platform in ('chat','chat_merge')` |
| 站外分享 PV/UV | `livesdk_share_v2.to_user_id='out_app_share'` 或平台不在 IM/公告板枚举 |
| IM 卡曝光 UV | `livesdk_share_chat_show` |
| IM 卡点击 UV | `livesdk_share_chat_click` |
| IM 回流进房 UV | `livesdk_live_play(is_ecom>0, enter_from_merge='chat')` |
| 站外回流进房 UV | `livesdk_live_play(is_ecom>0)` 且命中外部分享落地字段 |

直播站外字段要先用样本确认 `livesdk_live_play` 是否落表 `from_share_platform`。如果没有落表，不能拆站外 App 回流，只能用发送侧 App 分布 + 站外进房总量做对照。

### 看板 E：电商短视频分享 -> 短视频回流

推荐分两条主分支：

| 分支 | 发送侧漏斗 | 回流侧漏斗 | 归因方式 |
| --- | --- | --- | --- |
| 短视频 -> 站内 IM -> 视频播放 | `share_video_to_chat`，或 `share_video(platform='chat_head')` 作为补充 | `video_play(is_ecom=1, enter_from='chat', is_from_dm=1)` | 可按 `chat_type/conversation_id` 做 IM 内部分拆；具体消息级归因需消息/card trace |
| 短视频 -> 站外 App -> 视频播放 | `share_video(platform, share_link_id)` | `video_play(is_ecom=1, share_link_id is not null or ugdp_type is not null)` | 优先 `share_link_id` 精确 join；`ugdp_type` 只能说明打开方式，不是 App |

短视频看板建议指标：

| 指标 | 口径 |
| --- | --- |
| 视频分享 PV/UV | `share_video` / `share_video_to_chat` |
| 站外分享 PV/UV | `share_video.platform` 为站外渠道 |
| IM 分享 PV/UV | `share_video_to_chat` 或 IM 平台补充口径 |
| 站外回流播放 UV | `video_play(is_ecom=1)` 且 `share_link_id` 或 `ugdp_type` 命中 |
| IM 回流播放 UV | `video_play(is_ecom=1, enter_from='chat', is_from_dm=1)` |
| 回流后有效播放 | `play_time`，按 `duration` 阈值做 3s/5s/10s |
| 回流后完播 | `video_play_finish` |
| 回流后电商点击 | 商品锚点曝光/点击事件，按 `group_id + receiver_user_id + 时间窗` 关联 |

短视频如果要做“WhatsApp 回流 UV / Facebook 回流 UV”，必须用 `share_link_id` join `share_video.platform`。只有 `video_play.ugdp_type` 时不能拆 App。

### 推荐最终看板分组

实际 BI 页面建议做 5 个 tab：

| Tab | 内容 | 主要用途 |
| --- | --- | --- |
| `Overview` | 全场景 IM/站外分享与回流总览 | 看总体趋势和场景贡献 |
| `PDP` | PDP 分享点击、IM/站外发送、PDP 回流、全屏/半屏拆分 | 看商品详情页分享回流 |
| `Shop` | 店铺分享成功、IM/站外回流、`shop_share_reflow` 对账 | 看店铺详情页分享回流 |
| `Live` | 直播分享、IM 卡曝光/点击、进房回流 | 看直播 IM 卡和站外进房 |
| `Short Video` | 视频分享、deeplink 播放回流、回流后播放质量 | 看短视频分享带来的电商视频消费 |

## 不要这样统计

1. 不要把发送侧 `share_video`、`tiktokec_share_product`、`tiktokec_shop_share_success`、`livesdk_share_v2` 直接当成回流。它们表示分享动作，不表示有人回来了。
2. 不要把 PDP 的回流算到 `shop_share_reflow`。`shop_share_reflow` 是店铺回流服务端 metric。
3. 不要把 `livesdk_live_show` 当成直播进房。直播回流进入用 `livesdk_live_play`。
4. 不要把 `ugdp_type` 当成短视频具体分享 App。它是 deeplink 打开方式。
5. 不要把 `messenger`、`sms`、`email`、`instagram_dm` 算作 TikTok 站内 IM。它们是站外渠道。
6. 如果没有 `chain_key/share_link_id/shareLiveTraceId` 这类稳定 ID，不建议做“具体站外 App 回流 UV”精确看板，只能做发送侧分布和落地侧站外回流总量。

## 代码依据

| 场景 | 关键依据 |
| --- | --- |
| PDP | [[PDP 商品详情页分享漏斗埋点分析]] 和代码已确认 `tiktokec_enter_product_detail` 存在；但实际抓包未见时，需要先在日志/数仓校验。兜底候选是 `tiktokec_internal_enter_page_attribution_event` 与 `tiktokec_stay_product_detail`，请求类 `rd_tiktokec_product_detail_request_*` 只作诊断。如果这些都没有，站外链路可降到 `launch_log` / `deep_link_end` 统计唤端/路由成功，但不能当 PDP 页面落地。 |
| 店铺详情页 | [[店铺详情页分享漏斗埋点分析]] 已确认 `tiktokec_enter_shop.enter_from` 和 `shop_share_reflow(type)`；`ttec_shop_api` 只把 `shop_share_outside`、`chat_im`、`seller_qrscan` 计入店铺回流 metric。 |
| 直播 IM | `repos/TikTok/Modules/TikTokIM/AWEIM/Classes/Core/ChatMessage/CoreFeatures/ShareLive/AWEIMShareLiveComponent.swift`：IM 直播卡上报 `livesdk_share_chat_show`、`livesdk_live_show(enter_from_merge='chat', enter_method='live_cover')`，并透传 `from_unique_share_id`。 |
| 直播站外 | `repos/TikTok/Modules/TikTokLive/AWELive/Service/Factory/AWELiveMTRoomFactory.m`：外部分享链接识别 `share_app_id + share_link_id`，并透传 `from_share_platform`、`share_from_user_id`。 |
| 短视频 deeplink 回流 | `repos/TikTok/Modules/TikTokExternalRouter/AWEExternalRouter/Classes/Core/TTKDeeplinkTracker.m`：`videoPlayParamsForDeeplinkAweme` 输出 `ugdp_id`、`share_link_id`、`link_sharer_uid`、`ugdp_type`；`consumptionParamsForDeeplinkAwemeID` 给 `play_time/video_play_finish` 补充 `external_sharer_uid`。 |
| 短视频 IM | `repos/TikTok/Modules/TikTokIM/AWEIM/Classes/Core/ChatMessage/CoreFeatures/FeedInteraction/Component/AWEIMChatRoomFeedPlayerComponent.m`：从 IM 打开视频写入 `refer='chat'`、`enter_from='chat'`、`is_from_dm=1`、`chat_type`、`conversation_id`。 |
| 电商短视频识别 | `repos/TikTok/Modules/TikTokECommerce/TikTokECommerce/Classes/Core/ModuleService/SaaS/GECSaaSModuleService.m`：短视频 track 参数写入 `is_ecom`、`is_ec_video`、`ec_product_cnt`、`ecom_product_list`、`is_ecom_intent`。 |
