> [!important]
>
> **原文信息**
>
> 作者：神说要有光（神光的幸福生活）
>
> 日期：2025年12月22日

## 前言

我们和大模型聊天，可以问它一些问题，它告诉你怎么做。但是大模型没法帮你去做。

比如你想创建一个 react + vite 的 todolist 项目，你直接问大模型，它只能告诉你应该创建哪些文件，代码是什么，但是不能帮你读写文件、执行命令。

但是 cursor 是可以的——你让它创建一个 todolist 项目，它会直接给你写入文件。你还可以让它安装依赖，把项目跑起来。

**这是怎么实现的呢？**

开发一些 tool 交给 agent 调用就可以了。比如读文件、写文件、读取目录、创建目录、执行命令。

这节我们来学下 tool：

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66IicEtmwyPY7VUpP3MEJPwL4BcMUFs6Hz18icTLcrOreVCpsIP8dGyMJA/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=0)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66IicEtmwyPY7VUpP3MEJPwL4BcMUFs6Hz18icTLcrOreVCpsIP8dGyMJA/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 准备工作

### 1. 选择大模型

这里我们用阿里的千问，因为每个用户登录都有 100 万免费 token，够我们学习用了。

[![](https://mmbiz.qpic.cn/sz_mmbiz_jpg/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V669E0Rm2WvhxKlPXZsPNKXUba5OjVh1sPvhuROyFaPRjoLGdCN8WhX7g/640?wx_fmt=jpeg&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=1)](https://mmbiz.qpic.cn/sz_mmbiz_jpg/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V669E0Rm2WvhxKlPXZsPNKXUba5OjVh1sPvhuROyFaPRjoLGdCN8WhX7g/640?wx_fmt=jpeg&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=1)

> 当然，就算以后不免费了，买也没多少钱，几十块可以用很久了。你用别的大模型也一样，都可以。

### 2. 获取 API Key

登录百炼控制台：[https://bailian.console.aliyun.com/?tab=api#/api](https://bailian.console.aliyun.com/?tab=api#/api)

点击获取 API Key：

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66wdgutQ0nicwYYgrBbsoY5bV57RVh7GNmeuhwlzbcGr63o1bR6nzibZfw/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=2)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66wdgutQ0nicwYYgrBbsoY5bV57RVh7GNmeuhwlzbcGr63o1bR6nzibZfw/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=2)

### 3. 选择模型

搜索 coder 相关的编码模型，这里是生成代码用。每个模型训练的数据集不同，都是用于不同目的。

我们用 `qwen-coder-turbo` 这个就行。

### 4. 创建项目

```bash
mkdir tool-test
cd tool-test
npm init -y
```

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66tsm0rYfTS8Zu6ibGBHQpFf7lzuZXht0ZE8PTxD5PRnm4zhvZSib9yxiaA/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=3)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66tsm0rYfTS8Zu6ibGBHQpFf7lzuZXht0ZE8PTxD5PRnm4zhvZSib9yxiaA/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=3)

---

## 第一次调用大模型

用编辑器打开项目，创建文件 `src/hello-langchain.mjs`

> `.mjs` 是 ES Module 格式的 JS 文件，可以用 `import`、`export` 语法

```jsx
import { ChatOpenAI } from '@langchain/openai';

const model = new ChatOpenAI({
  modelName: "qwen-coder-turbo",
  apiKey: '你的 apiKey',
  configuration: {
    baseURL: 'https://dashscope.aliyuncs.com/compatible-mode/v1',
  },
});

const response = await model.invoke("介绍下自己");
console.log(response.content);
```

这里的 api key 换成你刚才复制的，base url 如下：

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66zfg22dXlOHUbqWjkG3ia4Z5wPjKkoDnl47rIAz2wDjEedHiaNTEsGXdg/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=4)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66zfg22dXlOHUbqWjkG3ia4Z5wPjKkoDnl47rIAz2wDjEedHiaNTEsGXdg/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=4)

安装依赖并运行：

```bash
pnpm install @langchain/openai
node ./src/hello-langchain.mjs
```

可以看到模型调用成功了。

---

## 使用环境变量管理 API Key

把 api key 写死到代码里的方式不好，我们通过 `.env` 文件来管理，然后用 `dotenv` 这个包来读取。

```bash
pnpm install dotenv
```

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V660Uu9SVZ13sl9a6EmfP15Fo7NW2ju5c6fy5yujffCcLjrItQIhRFT1g/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=5)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V660Uu9SVZ13sl9a6EmfP15Fo7NW2ju5c6fy5yujffCcLjrItQIhRFT1g/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=5)

`dotenv` 的作用就是读取 `.env` 文件，设置到环境变量里。

更新代码：

```jsx
import dotenv from 'dotenv';
import { ChatOpenAI } from '@langchain/openai';

dotenv.config();

const model = new ChatOpenAI({
  modelName: process.env.MODEL_NAME || "qwen-coder-turbo",
  apiKey: process.env.OPENAI_API_KEY,
  configuration: {
    baseURL: process.env.OPENAI_BASE_URL,
  },
});

const response = await model.invoke("介绍下自己");
console.log(response.content);
```

创建 `.env` 文件：

```bash
# OpenAI API 配置
OPENAI_API_KEY=你的 api key
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 模型配置（可选，默认为 qwen-coder-turbo）
MODEL_NAME=qwen-coder-turbo
```

> [!important]
>
> **重要**：将 `.env` 添加到 `.gitignore`，这些私密信息不保存到 git，就像数据库密码一样，都是私下里传文件，不会提交 git。

---

## 开发 Tool：读取文件

创建 `src/tool-file-read.mjs`：

```jsx
import 'dotenv/config';
import { ChatOpenAI } from '@langchain/openai';
import { tool } from '@langchain/core/tools';
import { HumanMessage, SystemMessage, ToolMessage } from '@langchain/core/messages';
import fs from 'node:fs/promises';
import { z } from 'zod';

const model = new ChatOpenAI({
  modelName: process.env.MODEL_NAME || "qwen-coder-turbo",
  apiKey: process.env.OPENAI_API_KEY,
  temperature: 0,
  configuration: {
    baseURL: process.env.OPENAI_BASE_URL,
  },
});

const readFileTool = tool(
  async ({ filePath }) => {
    const content = await fs.readFile(filePath, 'utf-8');
    console.log(`  [工具调用] read_file("${filePath}") - 成功读取 ${content.length} 字节`);
    return `文件内容:\n${content}`;
  },
  {
    name: 'read_file',
    description: '用此工具来读取文件内容。当用户要求读取文件、查看代码、分析文件内容时，调用此工具。输入文件路径（可以是相对路径或绝对路径）。',
    schema: z.object({
      filePath: z.string().describe('要读取的文件路径'),
    }),
  }
);

const tools = [readFileTool];
const modelWithTools = model.bindTools(tools);
```

安装依赖：

```bash
pnpm install @langchain/core zod
```

### 代码解析

**创建模型：**

- `temperature` 是温度，也就是 AI 的创造性，设置为 0，让它严格按照指令来做事情，不要自己发挥

- 引入 `dotenv/config` 模块即可自动加载环境变量

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66icRNib5OdXiblAZnG1gpYXaVD3LhLBEAyfM89wnGibRtdQ3fC11ib0U4NZw/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=6)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66icRNib5OdXiblAZnG1gpYXaVD3LhLBEAyfM89wnGibRtdQ3fC11ib0U4NZw/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=6)

**创建 Tool：**

调用 `tool` API，传入函数以及它的名字、描述、参数格式。因为要给大模型用，你要描述下这个工具是干什么的。

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V664HdElIselqB1iakUfgJIV5oLiaxf27Lav76LspblgDbn47H2H5bRVLtw/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=7)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V664HdElIselqB1iakUfgJIV5oLiaxf27Lav76LspblgDbn47H2H5bRVLtw/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=7)

这里用 `zod` 包来描述参数格式，就是传入一个 object，里面的 filePath 是一个 string：

```jsx
{
  filePath: 'xxx'
}
```

**绑定 Tool 到模型：**

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66LZiavJGkF6nqQoIqpia8eicTvYjaNX1obftdjS7q2ZQYDYdT82dCaA9icA/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=8)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66LZiavJGkF6nqQoIqpia8eicTvYjaNX1obftdjS7q2ZQYDYdT82dCaA9icA/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=8)

---

## 调用模型与处理 Tool Calls

### 消息类型

具体的消息有四种：

|类型|说明|
|---|---|
|**SystemMessage**|设置 AI 是谁，可以干什么，有什么能力，以及回答、行为的规范等|
|**HumanMessage**|用户输入的信息|
|**AIMessage**|AI 的回复信息|
|**ToolMessage**|调用工具的结果返回|

### 发送消息

```jsx
const messages = [
  new SystemMessage(`你是一个代码助手，可以使用工具读取文件并解释代码。

工作流程：
1. 用户要求读取文件时，立即调用 read_file 工具
2. 等待工具返回文件内容
3. 基于文件内容进行分析和解释

可用工具：
- read_file: 读取文件内容（使用此工具来获取文件内容）
`),
  new HumanMessage('请读取 src/tool-file-read.mjs 文件内容并解释代码')
];

let response = await modelWithTools.invoke(messages);
console.log(response);
```

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66p3JbOf7p4V7WPmW0iacXiaMrRtCpYNibGf78Lnt3S3ER86puD6v0XKDIQ/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=9)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66p3JbOf7p4V7WPmW0iacXiaMrRtCpYNibGf78Lnt3S3ER86puD6v0XKDIQ/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=9)

运行：

```bash
node ./src/tool-file-read.mjs
```

可以看到 AI 返回的消息是 AIMessage 实例，它返回了这个信息：

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66WeSdjYODHpGkXNvSuEHx2wxBayeicdrQQcwyJLUxvlNVYxbjxgCVeicQ/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=10)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66WeSdjYODHpGkXNvSuEHx2wxBayeicdrQQcwyJLUxvlNVYxbjxgCVeicQ/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=10)

就是解析出来我们给的路径，拼接了调用工具的参数。

---

## 执行工具调用

接下来我们基于这个参数调用下工具：

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66lLRQglDuo1coOdhKpibAPMOv2nV5Xny6TBeE9WewIqRLmY3YWgh26pA/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=11)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66lLRQglDuo1coOdhKpibAPMOv2nV5Xny6TBeE9WewIqRLmY3YWgh26pA/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=11)

根据 `tool_calls` 的数组，分别从 tools 数组里找到对应的工具，取出来 invoke，传入大模型解析出的参数。

最后把工具调用结果作为 ToolMessage 传给大模型，让它继续回答：

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66KcfQ2U05xwz7vW7txDNmKUrO14sbkzPkw5hIYGmHjCcHun03M2Jg4A/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=12)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66KcfQ2U05xwz7vW7txDNmKUrO14sbkzPkw5hIYGmHjCcHun03M2Jg4A/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=12)

> [!important]
>
> **注意**：这里要用 toolCall 对应的 id 来关联执行结果，也就是告诉大模型，你让我调用的哪个工具，返回的结果是什么。

### 完整的工具调用循环

```jsx
let response = await modelWithTools.invoke(messages);

messages.push(response);

while (response.tool_calls && response.tool_calls.length > 0) {
  console.log(`\n[检测到 ${response.tool_calls.length} 个工具调用]`);

  // 执行所有工具调用
  const toolResults = await Promise.all(
    response.tool_calls.map(async (toolCall) => {
      const tool = tools.find(t => t.name === toolCall.name);
      if (!tool) {
        return `错误: 找不到工具 ${toolCall.name}`;
      }

      console.log(`  [执行工具] ${toolCall.name}(${JSON.stringify(toolCall.args)})`);
      try {
        const result = await tool.invoke(toolCall.args);
        return result;
      } catch (error) {
        return `错误: ${error.message}`;
      }
    })
  );

  // 将工具结果添加到消息历史
  response.tool_calls.forEach((toolCall, index) => {
    messages.push(
      new ToolMessage({
        content: toolResults[index],
        tool_call_id: toolCall.id,
      })
    );
  });

  // 再次调用模型，传入工具结果
  response = await modelWithTools.invoke(messages);
}

console.log('\n[最终回复]');
console.log(response.content);
```

运行结果：

[![](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66chnNnvMCtKKHLGuYoM9sN1Ln5egoibPQPcXGkoBNxvia7o3feomboKvQ/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=13)](https://mmbiz.qpic.cn/sz_mmbiz_png/JnZIbTicL4jWMUiabDiamSHCjWPuYZq5V66chnNnvMCtKKHLGuYoM9sN1Ln5egoibPQPcXGkoBNxvia7o3feomboKvQ/640?wx_fmt=png&from=appmsg&tp=wxpic&wxfrom=5&wx_lazy=1#imgIndex=13)

可以看到，检测到了 tool_calls 工具调用，用 read_file 这个工具读取了文件，然后让大模型分析了文件内容，给出了代码解释。

**现在大模型就能读文件了！这就是通过工具给大模型扩展了能力。**

---

## 总结

这节我们入门了 langchain，调用了大模型，并且实现了第一个 tool。

### 要点回顾

1. **模型选择**：使用千问模型，有免费额度，获取 API Key 后用 `.env` 管理

1. **环境变量**：`.env` 文件不提交 git，都是私下传文件，就和数据库密码一样

1. **创建 Tool**：用 `tool` API 创建工具，写一下函数，加上名字、描述、参数格式（用 zod 声明）

1. **绑定工具**：用 `model.bindTools` 传给大模型，在 SystemMessage 告诉它工具信息和回答流程

1. **消息类型**：分为 SystemMessage、HumanMessage、AIMessage、ToolMessage 四种

### 下一步

实现了第一个 tool 之后，你可以想一下 cursor 怎么实现——后面我们实现一个简易版 cursor！

> [!important]
>
> **代码仓库**：[https://github.com/QuarkGluonPlasma/ai-agent-course-code](https://github.com/QuarkGluonPlasma/ai-agent-course-code)
