> 📚 **原文链接:** [极客时间专栏](https://time.geekbang.org/column/article/719462)

你好,我是黄佳,欢迎来到 LangChain 实战课!

上节课,咱们的聊天机器人已经基本完成,这节课,我们要看一看如何把它部署到网络上。

---

## "聊天机器人"项目回顾

简单回顾一下这个项目的设计:

1. **第一步:** 通过 LangChain 的 ConversationChain,实现一个最基本的聊天对话工具

1. **第二步:** 通过 LangChain 中的记忆功能,让聊天机器人能够记住用户之前所说的话

1. **第三步:** 通过 LangChain 中的检索功能,整合易速鲜花的内部文档资料

1. **第四步(可选):** 通过 LangChain 中的数据库查询功能,查询订单状态等

1. **第五步:** 在网络上部署及发布这个聊天机器人

### 部署框架的选择

在上一个项目中,我们是通过 **Flask** 部署的人脉工具。Flask 是一个通用的、微型的 Web 应用框架,为开发者提供了很高的灵活性。

不过,对于机器学习项目来说,我们还有其他部署方案。比如 **Streamlit** 和 **Gradio**,就为机器学习和数据科学应用提供了快速、专门化的解决方案。如果你的项目目标是快速展示和验证模型效果,那么它们是优秀的选择。

---

## 方案 1:通过 Streamlit 部署聊天机器人

Streamlit 是一个专门为数据科学家和机器学习工程师设计的开源 Python 库,它可以迅速地将 Python 脚本转化为交互式 Web 应用。

### Streamlit 主要特点

> [!important]
>
> - **简易性:** 只需几行代码,就可以创建交互式应用
>
> - **无需前端经验:** 不需要了解 HTML、CSS 或 JavaScript
>
> - **实时交互:** 更改代码或数据时,应用会实时更新
>
> - **内置组件:** 滑块、按钮、表格等可以无缝集成
>
> - **数据可视化:** 支持 Plotly、Matplotlib 和 Altair 等库
>
> - **部署和共享:** Streamlit Sharing 允许免费托管应用

[![](https://static001.geekbang.org/resource/image/71/6a/718fbd049acdf8681185805384028f6a.jpg?wh=1448x679)](https://static001.geekbang.org/resource/image/71/6a/718fbd049acdf8681185805384028f6a.jpg?wh=1448x679)

Streamlit 官网

### 安装与入门示例

```bash
pip install streamlit
```

简单几行代码就可以做出一个网页版小程序:

```python
import streamlit as st

st.title('平方计算器')
number = st.slider("Select a number:", min_value=0, max_value=100)
st.write(f"Square of {number} is {number ** 2}")
```

> [!important]
>
> **注意:** 必须使用 `streamlit run` 命令运行,而不是通过 `python` 命令。

```bash
streamlit run 01_SimpleStreamlit.py
```

[![](https://static001.geekbang.org/resource/image/22/ee/22e0fcf90978160e1f50d75fe9a33dee.jpg?wh=661x458)](https://static001.geekbang.org/resource/image/22/ee/22e0fcf90978160e1f50d75fe9a33dee.jpg?wh=661x458)

Streamlit 启动终端

[![](https://static001.geekbang.org/resource/image/ef/87/ef4e3a09a3df9776330b343e721fbe87.jpg?wh=1364x1189)](https://static001.geekbang.org/resource/image/ef/87/ef4e3a09a3df9776330b343e721fbe87.jpg?wh=1364x1189)

平方计算器运行效果

### 用 Streamlit 重构聊天机器人

```python
import os
import streamlit as st
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain.embeddings import OpenAIEmbeddings
from langchain.vectorstores import Qdrant
from langchain.memory import ConversationSummaryMemory
from langchain.chat_models import ChatOpenAI
from langchain.chains import ConversationalRetrievalChain
from langchain.document_loaders import PyPDFLoader
from langchain.document_loaders import Docx2txtLoader
from langchain.document_loaders import TextLoader

os.environ["OPENAI_API_KEY"] = 'Your OpenAI Key'

class ChatbotWithRetrieval:
    def __init__(self, dir):
        base_dir = dir
        documents = []
        for file in os.listdir(base_dir):
            file_path = os.path.join(base_dir, file)
            if file.endswith('.pdf'):
                loader = PyPDFLoader(file_path)
                documents.extend(loader.load())
            elif file.endswith('.docx') or file.endswith('.doc'):
                loader = Docx2txtLoader(file_path)
                documents.extend(loader.load())
            elif file.endswith('.txt'):
                loader = TextLoader(file_path)
                documents.extend(loader.load())

        text_splitter = RecursiveCharacterTextSplitter(chunk_size=200, chunk_overlap=0)
        all_splits = text_splitter.split_documents(documents)

        self.vectorstore = Qdrant.from_documents(
            documents=all_splits,
            embedding=OpenAIEmbeddings(),
            location=":memory:",
            collection_name="my_documents",
        )

        self.llm = ChatOpenAI()
        self.memory = ConversationSummaryMemory(
            llm=self.llm,
            memory_key="chat_history",
            return_messages=True
        )

        retriever = self.vectorstore.as_retriever()
        self.qa = ConversationalRetrievalChain.from_llm(
            self.llm,
            retriever=retriever,
            memory=self.memory
        )

def main():
    st.title("易速鲜花聊天客服")

    if "bot" not in st.session_state:
        st.session_state.bot = ChatbotWithRetrieval("OneFlower")

    user_input = st.text_input("请输入你的问题：")

    if user_input:
        response = st.session_state.bot.qa(user_input)
        st.write(f"Chatbot: {response['answer']}")

if __name__ == "__main__":
    main()
```

### Streamlit 关键代码说明

> [!important]
>
> **界面创建:** `st.title("易速鲜花聊天客服")` 设置 Web 应用程序的标题
>
> **会话状态:** 使用 `st.session_state` 存储用户会话状态,避免每次交互时重新初始化机器人
>
> **用户交互:** `st.text_input()` 创建文本输入框,`st.write()` 在界面上显示响应

运行程序,就可以开始聊天了!

[![](https://static001.geekbang.org/resource/image/1f/49/1f380b60c8cdec362c9c8070c919d849.jpg?wh=3840x2076)](https://static001.geekbang.org/resource/image/1f/49/1f380b60c8cdec362c9c8070c919d849.jpg?wh=3840x2076)

Streamlit 聊天机器人运行效果

---

## 方案 2:通过 Gradio 部署聊天机器人

与 Streamlit 不同,**Gradio** 界面更侧重于模型的交互,上手也更简单。这使得 Gradio 非常适合展示和测试机器学习模型。很多新的开源 LLM 都是提供一个 Gradio UI 界面来进行测试的。

### 安装

```bash
pip install gradio
```

### 用 Gradio 重构聊天机器人

```python
import os
import gradio as gr
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain.embeddings import OpenAIEmbeddings
from langchain.vectorstores import Qdrant
from langchain.memory import ConversationSummaryMemory
from langchain.chat_models import ChatOpenAI
from langchain.chains import ConversationalRetrievalChain
from langchain.document_loaders import PyPDFLoader
from langchain.document_loaders import Docx2txtLoader
from langchain.document_loaders import TextLoader

os.environ["OPENAI_API_KEY"] = 'Your OpenAI Key'

class ChatbotWithRetrieval:
    def __init__(self, dir):
        base_dir = dir
        documents = []
        for file in os.listdir(base_dir):
            file_path = os.path.join(base_dir, file)
            if file.endswith('.pdf'):
                loader = PyPDFLoader(file_path)
                documents.extend(loader.load())
            elif file.endswith('.docx') or file.endswith('.doc'):
                loader = Docx2txtLoader(file_path)
                documents.extend(loader.load())
            elif file.endswith('.txt'):
                loader = TextLoader(file_path)
                documents.extend(loader.load())

        text_splitter = RecursiveCharacterTextSplitter(chunk_size=200, chunk_overlap=0)
        all_splits = text_splitter.split_documents(documents)

        self.vectorstore = Qdrant.from_documents(
            documents=all_splits,
            embedding=OpenAIEmbeddings(),
            location=":memory:",
            collection_name="my_documents",
        )

        self.llm = ChatOpenAI()
        self.memory = ConversationSummaryMemory(
            llm=self.llm,
            memory_key="chat_history",
            return_messages=True
        )

        self.conversation_history = ""

        retriever = self.vectorstore.as_retriever()
        self.qa = ConversationalRetrievalChain.from_llm(
            self.llm,
            retriever=retriever,
            memory=self.memory
        )

    def get_response(self, user_input):
        response = self.qa(user_input)
        self.conversation_history += f"你: {user_input}\nChatbot: {response['answer']}\n"
        return self.conversation_history

if __name__ == "__main__":
    folder = "OneFlower"
    bot = ChatbotWithRetrieval(folder)

    interface = gr.Interface(
        fn=bot.get_response,
        inputs="text",
        outputs="text",
        live=False,
        title="易速鲜花智能客服",
        description="请输入问题，然后点击提交。"
    )
    interface.launch()
```

### Gradio 关键代码说明

> [!important]
>
> `**get_response(self, user_input)**`**:** 为 Gradio 创建的函数,接收用户输入并返回机器人的响应。每次将用户输入和响应添加到 `conversation_history`,保持聊天历史连续性。
>
> `**gr.Interface()**`**:** 定义 Gradio 界面。`live=False` 确保用户需要点击提交按钮发送问题,而不是一边打字一边生成回答。
>
> `**interface.launch()**`**:** 启动 Gradio 的 Web 服务器,在默认浏览器中打开界面。

运行程序,聊天机器人在本地端口 7860 上启动。

[![](https://static001.geekbang.org/resource/image/46/d8/460fa6854ffa68ef4bfa54056cb034d8.jpg?wh=2140x1499)](https://static001.geekbang.org/resource/image/46/d8/460fa6854ffa68ef4bfa54056cb034d8.jpg?wh=2140x1499)

Gradio 聊天机器人运行效果

输入输出窗口的配置更加清晰,而且增加了历史对话信息记录功能。

---

## 总结时刻

Streamlit 和 Gradio 都是让数据科学家和开发者能够快速为机器学习模型创建 Web UI 的框架。

> [!important]
>
> **Streamlit** — 为数据应用、仪表板和可视化设计。提供了多种小部件,非常 Pythonic,对于熟悉 Python 的人来说非常直观。
>
> **Gradio** — 更多是为了展示和演示机器学习模型。提供了一种快速的方法,使非技术用户也能与机器学习模型进行交互。

[![](https://static001.geekbang.org/resource/image/65/87/654d95bdb96e627c2464b4cedd964e87.jpg?wh=1110x752)](https://static001.geekbang.org/resource/image/65/87/654d95bdb96e627c2464b4cedd964e87.jpg?wh=1110x752)

Streamlit vs Gradio 对比表

无论选择哪个框架,你都可以在非常短的时间内为你的应用创建一个 Web UI。至于选择哪个,更多取决于你的具体需求和个人喜好。

---

## 思考题

1. 我的易速鲜花 Chatbot 有很多不完美的地方,比如检索功能的设计不够细致,UI 不够美观等等。请你在这个 Repo 的基础上,大刀阔斧地进行改进。

1. 请你用 Flask 框架设计自己的 Chatbot UI,重构聊天机器人,实现更多、更完善的功能。

1. 请你回过头去看看第 01 讲我给你留的 3 道思考题。那时候,你不了解 LangChain,现在你已经基本掌握了它的精髓,能否把第 01 讲的思考题重新回答一遍呢?应该很有趣吧!

期待在留言区看到你的成果分享,如果觉得内容对你有帮助,也欢迎分享给有需要的朋友!


课程封面
